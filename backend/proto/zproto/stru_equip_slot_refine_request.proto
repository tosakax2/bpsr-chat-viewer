syntax = "proto3";

package zproto;

import "stru_equip_slot_refine_item_cost.proto";

message EquipSlotRefineRequest {
  uint32 slot_id = 1;
  repeated zproto.EquipSlotRefineItemCost item_cost_list = 2;
}

